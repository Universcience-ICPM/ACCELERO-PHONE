Place the impact sound here as either:

- `collision.mp3`
- `collision.wav`

The controller page tries `collision.mp3` first, then `collision.wav`.
For a one-off test with another filename, open `/controller?sound=my-file.mp3`.
Use `/controller?soundVolume=1` to test at maximum Web Audio volume.
